import { OptionsFromEnumPipe } from './options-from-enum.pipe';

describe('OptionsFromEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionsFromEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
